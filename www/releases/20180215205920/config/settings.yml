# config/app.yml for rails-settings-cached
defaults: &defaults
  beacon_range_near_meters: 3
  crossing_grace_period_seconds: 300
  detected_corner_strategy: 'average'
  beacon_update_rate: 1000



development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
